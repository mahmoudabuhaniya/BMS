# Environment variables
.env

# Other common ignores for Django/Flutter (add these for completeness)
*.pyc
__pycache__/
*.pyo
*.pyd
.Python
env/
venv/
*.db
*.sqlite3
media/
staticfiles/
# Flutter-specific
.flutter-plugins
.flutter-plugins-dependencies
build/
.dart_tool/
.pub-cache/
# Remove the old .git folder completely (this deletes all history — safe because we're starting fresh)
Remove-Item -Recurse -Force .git

# Create a fresh .gitignore to block large/unneeded files
@"
# Django
__pycache__/
*.py[cod]
*.sqlite3
db.sqlite3
venv/
.env
*.env

# Flutter/Android
build/
android/app/build/
my_mobile_app_old/
*.zip
*.tar.xz
*.deb
*.apk
*.aab

# Static/Media (if large)
staticfiles/
media/

# IDE
.vscode/
.idea/

# Logs
*.log
"@ | Out-File -Encoding utf8 .gitignore